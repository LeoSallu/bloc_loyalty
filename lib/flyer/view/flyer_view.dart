import 'package:flutter/material.dart';

class FlyerView extends StatelessWidget {
  const FlyerView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('PAGINA VOLANTINO'),
      ),
    );
  }
}
